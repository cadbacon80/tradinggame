# Copy to .env.local and fill in your Supabase project values.
# Both are safe to expose in client code (anon key only).
# Without these set, the app runs in OFFLINE mode (solo play only).

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
